# مدیریت داده‌های ویدیو - راهنمای استفاده

این پروژه شامل دو فایل اصلی است:

## 📁 فایل‌ها

### 1. `video_data_manager.py` (ماژول)
ماژولی که شامل کلاس `VideoDataManager` برای مدیریت و پردازش داده‌های ویدیو است.

**ویژگی‌های کلاس:**
- `__init__(file_path)`: دریافت مسیر فایل در constructor
- `get_video_name()`: پیدا کردن نام ویدیو (هر فایل شامل یک ویدیوی مشخص است)
- `get_frame_number(line_index=0)`: پیدا کردن شماره فریم از یک خط خاص
- `get_float_values()`: دریافت لیست مقادیر float از فایل
- `get_all_video_names()`: دریافت نام ویدیو (به صورت لیست)
- `get_all_frame_numbers()`: دریافت لیست تمام شماره‌های فریم
- `get_data_shape()`: دریافت ابعاد داده‌ها (تعداد فریم × تعداد ویژگی)
- `get_frame_by_number(frame_number)`: دریافت اطلاعات یک فریم خاص

### 2. `main.py` (برنامه اصلی)
برنامه‌ای که تمرین را اجرا می‌کند:
1. لیستی از فایل‌های txt در دایرکتوری `./vids_feats` ایجاد می‌کند
2. در یک حلقه for تک تک فایل‌ها را خط به خط می‌خواند
3. از کلاس `VideoDataManager` برای پردازش استفاده می‌کند
4. اطلاعات دقیق هر فایل را نمایش می‌دهد

## 🚀 نحوه استفاده

### پیش‌نیاز
فایل‌های txt باید در دایرکتوری `./vids_feats` قرار داشته باشند.

### اجرای برنامه
```bash
python main.py
```

## 📊 فرمت داده‌ها

هر خط در فایل‌های txt به این فرمت است:
```
vid[شماره_ویدیو]_frame_[شماره_فریم],value1,value2,value3,...
```

مثال:
```
vid1202_frame_18,0.095801,0.199193,1.311035,...
```

## 🎯 مثال استفاده از کلاس

```python
from video_data_manager import VideoDataManager

# ایجاد شی
manager = VideoDataManager("./vids_feats/feats1.txt")

# خواندن داده‌ها
data = manager.get_float_values()

# دریافت نام ویدیو (هر فایل = یک ویدیو)
video_name = manager.get_video_name()
print(f"نام ویدیو: {video_name}")  # خروجی: vid1202

# دریافت شماره فریم اول
first_frame_num = manager.get_frame_number(0)
print(f"شماره فریم اول: {first_frame_num}")

# دریافت ابعاد داده‌ها
num_frames, num_features = manager.get_data_shape()
print(f"{num_frames} فریم × {num_features} ویژگی")

# دریافت اطلاعات یک فریم خاص (مثلاً فریم 18)
frame_info = manager.get_frame_by_number(18)
if frame_info:
    print(f"فریم {frame_info['frame_number']}: {len(frame_info['features'])} ویژگی")
```

## 📝 نکات مهم

- ⭐ **هر فایل فقط شامل فریم‌های یک ویدیوی مشخص است**
  - feats1.txt → vid1202
  - feats2.txt → vid1203
  - feats3.txt → vid1201
  - feats4.txt → vid1204
- هر فریم دارای 2048 ویژگی (feature) است
- برنامه به صورت خودکار نام ویدیو و شماره فریم را از هر خط استخراج می‌کند

## ✅ خروجی نمونه

```
======================================================================
در حال پردازش: feats1.txt
======================================================================

📊 اطلاعات فایل:
   - نام ویدیو: vid1202
   - تعداد کل فریم‌ها: 54
   - محدوده شماره فریم‌ها: 1 - 54
   - تعداد ویژگی‌ها در هر فریم: 2048

🎬 نمونه‌ای از فریم اول:
   - نام فریم: vid1202_frame_18
   - شماره فریم: 18

VideoDataManager(
  فایل: feats1.txt
  نام ویدیو: vid1202
  تعداد فریم‌ها: 54
  تعداد ویژگی‌ها در هر فریم: 2048
  محدوده شماره فریم‌ها: 1 - 54
)

======================================================================
📈 خلاصه کلی:
======================================================================
تعداد کل فایل‌های پردازش شده: 4
تعداد کل فریم‌ها در همه فایل‌ها: 148
تعداد ویدیوها: 4
نام‌های ویدیو: vid1202, vid1203, vid1201, vid1204

📁 جزئیات هر فایل:
  - feats1.txt (vid1202): 54 فریم × 2048 ویژگی
  - feats2.txt (vid1203): 35 فریم × 2048 ویژگی
  - feats3.txt (vid1201): 21 فریم × 2048 ویژگی
  - feats4.txt (vid1204): 38 فریم × 2048 ویژگی
```
